#pragma comment(lib, "engine.lib")
#include "../Engine/__engine.h"
#include "Components.h"

// #define _CRTDBG_MAP_ALLOC
// #include <cstdlib>
// #include <crtdbg.h>

int main()
{

	int a = 1;
	// auto scene0_idx = new_scene();
	// auto scene1_idx = new_scene();
	// auto scene2_idx = new_scene();
	// auto scene3_idx = new_scene();

	// auto world0_scene0_idx = new_world(scene0_idx);
	// auto world1_scene0_idx = new_world(scene0_idx);
	// auto world2_scene0_idx = new_world(scene0_idx);
	// auto world3_scene0_idx = new_world(scene0_idx);

	// auto world0_scene1_idx = new_world(scene1_idx);
	// auto world1_scene1_idx = new_world(scene1_idx);
	// auto world2_scene1_idx = new_world(scene1_idx);
	// auto world3_scene1_idx = new_world(scene1_idx);

	// auto world0_scene2_idx = new_world(scene2_idx);
	// auto world1_scene2_idx = new_world(scene2_idx);
	// auto world2_scene2_idx = new_world(scene2_idx);
	// auto world3_scene2_idx = new_world(scene2_idx);

	// auto world0_scene3_idx = new_world(scene3_idx);
	// auto world1_scene3_idx = new_world(scene3_idx);
	// auto world2_scene3_idx = new_world(scene3_idx);
	// auto world3_scene3_idx = new_world(scene3_idx);
	//_CrtDumpMemoryLeaks();
	return 0;
}