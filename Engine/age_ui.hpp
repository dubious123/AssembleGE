#pragma once
#include "age.hpp"

namespace age::ui::e
{
	AGE_DEFINE_ENUM(shape_kind, uint32,
					rect,
					circle);

	AGE_DEFINE_ENUM(brush_kind, uint32,
					color);
}	 // namespace age::ui::e

namespace age::ui
{

}